# Apply Color Mapping (Quantitative or Categorical)

Apply Color Mapping (Quantitative or Categorical)

## Usage

``` r
.apply_color_mapping(
  p,
  layoutDf,
  color.by,
  color.palette,
  color.legend.title,
  geom_type = c("color", "fill"),
  unenrichedColor = "#CCCCCC"
)
```

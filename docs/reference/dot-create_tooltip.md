# Create Tooltip HTML for visNetwork

Create Tooltip HTML for visNetwork

## Usage

``` r
.create_tooltip(
  complexId,
  primaryFunctionalDomain,
  proteinCount,
  proteins,
  score = NULL,
  color.by = NULL,
  color.value = NULL
)
```

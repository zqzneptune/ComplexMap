# Get the Edge Table from a ComplexMap Object

Access the network edge list (weights, similarity types).

## Usage

``` r
getEdgeTable(cm)
```

## Arguments

- cm:

  A \`ComplexMap\` object.

## Value

A \`tibble\`.

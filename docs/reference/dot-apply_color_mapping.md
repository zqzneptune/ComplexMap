# Apply Color Mapping (Categorical or Continuous)

Apply Color Mapping (Categorical or Continuous)

## Usage

``` r
.apply_color_mapping(
  p,
  layoutDf,
  color.by,
  color.palette,
  color.legend.title,
  geom_type = c("color", "fill"),
  unenrichedColor = "#CCCCCC"
)
```

# Apply Common Theme to ggraph Plot

Apply Common Theme to ggraph Plot

## Usage

``` r
.apply_theme(
  p,
  bgColor,
  edgeColor,
  fontFamily,
  nodeSizeRange,
  size.legend.title,
  title,
  subtitle
)
```
